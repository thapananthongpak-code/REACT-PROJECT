const Footer = ({MyShop}) => (
  <footer className="bg-black text-white p-6 flex justify-center items-center">
    <span>{MyShop}</span>
    <span>{MyShop}</span>

  </footer>
);

export default Footer;